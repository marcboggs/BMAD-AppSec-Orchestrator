<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Threat Model: {{COMPONENT_NAME}}</title>
<script src="https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js"></script>
<style>
  :root {
    --bg: #0f1115; --panel: #161922; --border: #262b38; --text: #e6e8ee;
    --muted: #9aa3b2; --accent: #5b8def;
    --crit: #ef4444; --high: #f59e0b; --med: #eab308; --low: #22c55e;
  }
  * { box-sizing: border-box; }
  body {
    margin: 0; background: var(--bg); color: var(--text);
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
    line-height: 1.55;
  }
  .wrap { max-width: 980px; margin: 0 auto; padding: 40px 24px 80px; }
  header { border-bottom: 1px solid var(--border); padding-bottom: 20px; margin-bottom: 28px; }
  h1 { font-size: 1.7rem; margin: 0 0 8px; }
  .meta { color: var(--muted); font-size: 0.9rem; }
  .meta span { margin-right: 18px; }
  h2 { font-size: 1.2rem; margin-top: 40px; border-left: 3px solid var(--accent); padding-left: 10px; }
  section { margin-bottom: 8px; }
  table { width: 100%; border-collapse: collapse; margin: 14px 0; font-size: 0.92rem; }
  th, td { border: 1px solid var(--border); padding: 8px 10px; text-align: left; vertical-align: top; }
  th { background: var(--panel); color: var(--muted); font-weight: 600; }
  tr:nth-child(even) td { background: rgba(255,255,255,0.02); }
  .badge { display: inline-block; padding: 2px 8px; border-radius: 4px; font-size: 0.78rem; font-weight: 600; color: #0f1115; }
  .sev-critical { background: var(--crit); color: #fff; }
  .sev-high { background: var(--high); }
  .sev-medium { background: var(--med); }
  .sev-low { background: var(--low); }
  .mermaid { background: var(--panel); border: 1px solid var(--border); border-radius: 8px; padding: 16px; margin: 16px 0; }
  .card { background: var(--panel); border: 1px solid var(--border); border-radius: 8px; padding: 16px 18px; margin: 14px 0; }
  code { background: var(--panel); padding: 1px 5px; border-radius: 4px; font-size: 0.9em; }
  footer { margin-top: 60px; color: var(--muted); font-size: 0.8rem; border-top: 1px solid var(--border); padding-top: 14px; }
</style>
</head>
<body>
<div class="wrap">

  <header>
    <h1>Threat Model: {{COMPONENT_NAME}}</h1>
    <div class="meta">
      <span><strong>Scope:</strong> {{SCOPE_DESCRIPTION}}</span>
      <span><strong>Date:</strong> {{DATE}}</span>
      <span><strong>Repo/commit:</strong> {{REPO_REF}}</span>
    </div>
  </header>

  <section>
    <h2>1. Executive Summary</h2>
    <div class="card">{{EXEC_SUMMARY}}</div>
  </section>

  <section>
    <h2>2. Scan Coverage</h2>
    <table>
      <tr><th>Source</th><th>Status</th></tr>
      <tr><td>secreview SAST</td><td>{{SAST_STATUS}}</td></tr>
      <tr><td>secreview SCA</td><td>{{SCA_STATUS}}</td></tr>
    </table>
  </section>

  <section>
    <h2>3. Architecture & Data Flow</h2>
    <p>{{ARCHITECTURE_DESCRIPTION}}</p>
    <div class="mermaid">
{{MERMAID_ARCHITECTURE_DIAGRAM}}
    </div>
    <h3>High-risk flow detail</h3>
    <div class="mermaid">
{{MERMAID_SEQUENCE_DIAGRAM}}
    </div>
  </section>

  <section>
    <h2>4. Component Inventory</h2>
    <table>
      <tr><th>Component</th><th>Purpose</th><th>Trust boundary</th><th>Notes</th></tr>
      {{COMPONENT_ROWS}}
    </table>
  </section>

  <section>
    <h2>5. STRIDE Threat Matrix</h2>
    <table>
      <tr><th>#</th><th>Component/Flow</th><th>STRIDE</th><th>Threat</th><th>secreview finding</th><th>Severity</th><th>Mitigation</th></tr>
      {{THREAT_MATRIX_ROWS}}
      <!-- Example row (remove once populated):
      <tr>
        <td>T1</td><td>API → Service</td><td>Elevation of Privilege</td>
        <td>Missing authz check on internal endpoint</td>
        <td>CWE-862 (SAST-014)</td>
        <td><span class="badge sev-high">High</span></td>
        <td>Enforce role check server-side</td>
      </tr>
      -->
    </table>
  </section>

  <section>
    <h2>6. secreview Findings Detail</h2>
    <h3>SAST</h3>
    <table>
      <tr><th>ID</th><th>CWE</th><th>Severity</th><th>File:Line</th><th>Description</th></tr>
      {{SAST_ROWS}}
    </table>
    <h3>SCA</h3>
    <table>
      <tr><th>ID</th><th>Package</th><th>CVE</th><th>Severity</th><th>Direct/Transitive</th><th>Description</th></tr>
      {{SCA_ROWS}}
    </table>
  </section>

  <section>
    <h2>7. Recommendations (Prioritized)</h2>
    <div class="card">{{RECOMMENDATIONS}}</div>
  </section>

  <section>
    <h2>8. Assumptions & Out-of-Scope</h2>
    <div class="card">{{ASSUMPTIONS}}</div>
  </section>

  <footer>Generated by the threat-model Kiro agent, informed by secreview SAST/SCA output.</footer>
</div>

<script>
  mermaid.initialize({ startOnLoad: true, theme: "dark" });
</script>
</body>
</html>
